{
    "name": "pair-heap",
    "version": "1.0.0",
    "scripts": {
        "start": "node dist/index.js",
        "build": "rimraf dist && npx tsc"
    },
    "devDependencies": {
        "rimraf": "^6.0.1",
        "typescript": "^5.6.3"
    }
}
