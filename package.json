{
    "name": "pair-heap",
    "version": "1.0.0",
    "scripts": {
        "start": "node dist/index.js",
        "build": "rimraf dist && npx tsc"
    },
    "devDependencies": {
        "rimraf": "^6.0.1",
        "typescript": "^5.6.3"
    },
    "dependencies": {
        "@types/node": "^22.7.7",
        "axios": "^1.7.7"
    }
}
